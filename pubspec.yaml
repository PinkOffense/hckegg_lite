name: chicken_coop_manager
description: "Gestão Pessoal de Galinheiro - Meu Galinheiro"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Flutter-provided localization package (required for Material localizations)
  flutter_localizations:
    sdk: flutter

  # UI / utilities
  cupertino_icons: ^1.0.8
  provider: ^6.0.5
  google_fonts: ^6.3.2

  # Database
  sqflite: ^2.3.0
  path: ^1.8.3

  # Charts
  fl_chart: ^0.65.0

  # Image handling
  image_picker: ^1.0.4

  # Notifications
  flutter_local_notifications: ^16.1.0

  # Additional utilities
  intl: ^0.19.0
  path_provider: ^2.1.1
  csv: ^5.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
